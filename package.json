{
  "name": "quiet-dictation-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "test:android": "npm run cap:sync && cd android && ./gradlew --no-daemon testDebugUnitTest",
    "test:claim:on-device-speech": "npm test -- -t @claim:on-device-speech && npm run test:android -- --tests in.sociobot.quietdictationbridge.LocalSpeechPermissionTest --tests in.sociobot.quietdictationbridge.HoldSessionTest",
    "test:e2e": "npm run build && playwright test",
    "cap:sync": "npm run build && cap sync android",
    "lint:android": "cd android && ./gradlew --no-daemon lint",
    "package:android": "node scripts/package-android.mjs",
    "verify:billing": "node scripts/verify-billing.mjs",
    "cap:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3",
    "@playwright/test": "1.58.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
