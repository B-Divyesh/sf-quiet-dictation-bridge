{
  "name": "quiet-dictation-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "cap:sync": "npm run build && cap sync android",
    "package:android": "npm run cap:sync && cd android && ./gradlew --no-daemon assembleDebug && mkdir -p ../dist/download && cp app/build/outputs/apk/debug/app-debug.apk ../dist/download/quiet-dictation-bridge-debug.apk && cd ../dist/download && sha256sum quiet-dictation-bridge-debug.apk > quiet-dictation-bridge-debug.apk.sha256",
    "cap:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3",
    "@playwright/test": "1.58.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
